<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TakeNote</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
     <link
    href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css"
    rel="stylesheet"
/>
</head>
<body>

    <div class="main w-full min-h-screen bg-blue-300">
       
        <div class="form p-10 text-white">
             <h1 class="mt-10 text-white bg-blue-800 text-2xl font-bold text-center rounded-lg p-5 w-fit mx-auto px-20">TakeNote</h1>

            <form class="mt-15" action="/create" method="post">
                <input class="block w-full rounded-lg px-5 py-2 bg-zinc-800 " type="text" placeholder="title goes here" name="title" required>
                <textarea class="block w-full rounded-lg px-5 mt-2 py-2 resize-none bg-zinc-800 " placeholder="write your task details here" name="details"></textarea>
                <input class="mt-2 rounded-lg  bg-blue-600 px-5 py-2" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks p-10 flex gap-4 flex-wrap">
            <%if(files.length>0) {%>
                <% files.forEach(function(val){%>
                    <div class="task min-w-72  rounded-lg w-72 px-3 py-4 bg-zinc-800">
                    <h1 class="text-white text-2xl tracking-tigher"><%=val%></h1>
                    <div class="flex w-full justify-between items-center mt-3">
                        <a class="text-yellow-300 inline-block " href="/files/<%=val%>">Read More...</a>
                        <a href="/edit/<%=val%>">
                        <i class="ri-file-edit-line  ml-5 text-yellow-500 px-5 cursor-pointer hover:text-yellow-600"> Edit Filename</i>
                        </a>
                    </div>
                    
                    </div>
            
                <%})%>
            <%} else { %>
                <h3 class="text-zinc-600">no tasks yet...</h3>
            <%}%>  
            
            
        
        </div>
    </div>
</body>
</html>